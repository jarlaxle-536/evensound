from .loader import *

class Composition(Singleton):
    title = 'Composition title',
        gui_classes = [
            'CompositionTitleLabel',
            'CompositionTitleRowInput',
        ]
    )
    fields = [
        'title'
    ]
